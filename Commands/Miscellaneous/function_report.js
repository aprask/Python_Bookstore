document.getElementById("closeButton").addEventListener("click", function()
{
    window.close();
});
// simple function that closes the html window (activated by a pointer click)
// the reference id = "closeButton"